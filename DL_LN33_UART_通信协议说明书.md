# DL-LN33 UART 通信协议说明书

> 适用对象：DL-LN33 / DL-LN3X 自组网模块的 MCU、PC 上位机开发人员  
> 整理日期：2026-07-18  
> 来源：[基于 STM32F103 最小系统板和 DL-LN33 2.4G 通信 ZigBee 无线串口自组网采集温湿度](https://blog.csdn.net/qq_42250136/article/details/139967636)

## 1. 适用边界

本文将公开原文中可文字核验的 UART 协议内容整理为工程实施参考。无线 CRC、自动重传和多跳路由由模块处理；本文定义的是 MCU/PC 与模块之间的 UART 接口。

原文中一部分内部端口命令表以图片呈现，无法从页面文字可靠复原的命令码、响应字段和错误码，本文不会推定，并会明确标注“待确认”。本文不是模块制造商的正式数据手册。

## 2. 组网与 UART 参数

### 2.1 组网前提

| 项目 | 约束 / 行为 |
|---|---|
| 入网条件 | 各节点地址不同；信道和网络 ID 相同。 |
| 网络 ID | 不得为 `0x0000` 或 `0xFFFF`。 |
| 信道 | `0x0B` 至 `0x1A`。 |
| 路由收敛 | 节点刚启动后约需 1-5 分钟重建路由；这期间使用洪泛路由，速度较慢。 |
| 网络规模 | 典型约 190 个节点，最大 15 跳。 |
| 无线可靠性 | 无线链路使用 CRC；原文说明最多重传 15 次，高负载时至少 5 次。 |

### 2.2 UART 参数

| 参数 | 取值 |
|---|---|
| 字符格式 | 8 数据位、1 起始位、1 停止位、无校验（8N1） |
| 可选波特率 | 2400 / 4800 / 9600 / 14400 / 19200 / 28800 / 38400 / 57600 / 115200 / 230400 / 125000 / 250000 / 500000 |
| UART 校验 | 原文未描述 UART 层校验和或 CRC；完整性依赖起止符、长度和反转义后的计数校验。 |
| 电平注意 | 模块工作电压为 2.5-3.6 V；PC 串口需经 MAX3232 等电平转换器连接。 |

## 3. UART 帧格式

多字节地址采用小端序。地址 `0x0003` 在线上表示为 `03 00`。

```text
FE | LEN | SRC_PORT | DST_PORT | ADDR_L | ADDR_H | PAYLOAD[LEN-4] | FF
```

| 字段 | 长度 | 格式 | 说明 |
|---|---:|---|---|
| `SOF` | 1 | `FE` | 帧开始符。 |
| `LEN` | 1 | `04`-`3F` | 逻辑数据区长度；不包含 `SOF`、`LEN`、`EOF`，且不受转义后字节数影响。 |
| `SRC_PORT` | 1 | `00`-`FF` | 源端口。应用 MCU/PC 发送时必须大于等于 `0x80`。 |
| `DST_PORT` | 1 | `00`-`FF` | 目的端口；小于 `0x80` 时由模块内部程序处理。 |
| `ADDR_L` | 1 | `00`-`FF` | 对端地址低 8 位。 |
| `ADDR_H` | 1 | `00`-`FF` | 对端地址高 8 位。 |
| `PAYLOAD` | 0-59 | `00`-`FF` | 应用数据或内部端口命令。 |
| `EOF` | 1 | `FF` | 仅在已收到恰好 `LEN` 个逻辑数据字节时有效。 |

逻辑数据区长度关系：`LEN = 4 + PAYLOAD 长度`。

原文称单包最大长度为 63 Byte，随附代码也使用 63 字节逻辑缓冲区，因此可推得应用 `PAYLOAD` 最大为 59 字节。

### 示例：发送普通数据

目标地址为 `0x0003`，源端口为 `0x91`，目的端口为 `0x90`，数据为 `00 AE 13 33`：

```text
逻辑数据区：91 90 03 00 00 AE 13 33
LEN：08
线上帧：FE 08 91 90 03 00 00 AE 13 33 FF
```

## 4. 转义与反转义

端口、地址和 `PAYLOAD` 中出现边界字节时必须转义；`LEN` 不需要转义。

| 逻辑字节 | 线上字节 | 接收端还原 |
|---|---|---|
| `FF` | `FE FD` | 转义前缀后收到 `FD`，按 8 位字节加 2 还原为 `FF`。 |
| `FE` | `FE FC` | 转义前缀后收到 `FC`，按 8 位字节加 2 还原为 `FE`。 |
| 其他 | 保持原值 | 直接装入逻辑数据区。 |

`PAYLOAD = 09 FF` 的发送示例：

```text
逻辑数据区：91 90 03 00 09 FF
LEN：06
线上帧：FE 06 91 90 03 00 09 FE FD FF
```

### 接收状态机要求

1. 等待 `SOF (FE)`。
2. 读取 `LEN`，仅接受 `0x04` 至 `0x3F`。
3. 按转义规则累计 `LEN` 个**逻辑字节**。
4. 仅当随后收到 `EOF (FF)` 时提交该帧。
5. 提前 `EOF`、超长、非法长度、残缺转义或超时，均丢弃当前帧并重新等待 `SOF`。

建议将 UART 中断/DMA 的字节收集与业务处理分离，使用双缓冲或消息队列传递完整帧。

## 5. 地址与端口语义

### 5.1 地址字段方向

| 方向 | `ADDR_L/H` 的语义 |
|---|---|
| 本地 UART -> 本地模块 | 目标（远程）模块地址。 |
| 远程模块 -> 本地 UART | 远端发送模块地址；接收方看到的地址由原目标地址替换为发送方地址。 |

### 5.2 端口范围

| 范围 | 用途与限制 |
|---|---|
| `0x00`-`0x7F` | 模块保留端口。作为目的端口时由模块内部程序处理；作为应用发送的源端口时，报文无法发出。 |
| `0x80`-`0xFF` | UART 连接的 MCU 或 PC 可用端口。应按应用功能分配并固化。 |

### 示例：温度传输

节点 `0x0001` 向节点 `0x000F` 的应用端口 `A0` 发送温度 `0x17`：

```text
发送端 UART：FE 05 A0 A0 0F 00 17 FF
接收端 UART：FE 05 A0 A0 01 00 17 FF
```

接收帧中的地址 `01 00` 表示发送方节点 `0x0001`。

不建议：

- 用小于 `0x80` 的端口作为源端口；模块会返回错误报告。
- 向自己的开放端口发送数据；这会让 MCU 收到自己发出的内容，没有实际收益。

## 6. 已知内部端口

### 6.1 `0x20`：红色 LED 定位控制

目的端口 `0x20` 控制模块红色 LED，可向本地或远程节点发送，用于确认节点在线或定位特定地址模块。

```text
让远程节点 0x0002 点亮约 5 秒（原文示例）
FE 05 A3 20 02 00 32 FF
```

原文还有“`0x0A` 点亮 1 秒”的示例，因此 `0x32 = 50` 可合理推定时间单位约为 0.1 秒。

> 待确认：原文另一处将 `payload = 0x01` 描述为点亮时间，和上述推导不完全一致。0 值、最大值与超时行为应以原厂手册或台架实测为准。

### 6.2 `0x21`：基本信息管理（仅本地）

端口 `0x21` 用于地址、网络 ID、信道和波特率管理。其目标地址必须为本地地址 `0x0000`，只能通过当前模块 UART 配置，不能远程修改。

若不清楚当前波特率，原文建议将 `BaudReset` 引脚接 GND 后，以 115200 波特率配置。

| 操作 | 完整帧 | 含义 |
|---|---|---|
| 读地址 | `FE 05 90 21 00 00 01 FF` | 向本地管理端口请求地址。 |
| 读网络 ID | `FE 05 90 21 00 00 02 FF` | 请求网络 ID。 |
| 读信道 | `FE 05 90 21 00 00 03 FF` | 请求信道。 |
| 读波特率 | `FE 05 90 21 00 00 04 FF` | 请求 UART 波特率。 |
| 确认并重启 | `FE 05 90 21 00 00 10 FF` | 配置确认无误后发送；模块重启并使用新参数。 |

原文给出的配置成功响应示例：

```text
FE 05 21 90 00 00 00 FF
```

该帧表示管理端口 `0x21` 向原源端口 `0x90` 返回状态 `0x00`。

> 待确认：各“写配置”命令的完整参数、读取响应的数据格式、非零状态码尚需原厂资料确认。

### 6.3 `0x22`：错误报告

端口 `0x22` 用于报告通信错误，用户不应主动向它发送数据。

```text
错误触发：FE 05 20 20 02 00 32 FF   （源端口 0x20 非法）
错误上报：FE 06 22 20 02 00 E0 20 FF
```

从示例可知，错误上报源端口为 `0x22`，`PAYLOAD` 为 `E0 20`；`E0` 的完整错误码含义需要原厂错误码表确认。

## 7. 实现流程

### 7.1 发送端

1. 确认节点地址唯一、网络 ID 和信道与目标网络一致。
2. 选择 `0x80`-`0xFF` 的应用源端口，设置目的端口、目标地址与数据。
3. 按 `LEN = 4 + payload_len` 计算长度，并确保范围为 `0x04`-`0x3F`。
4. 对端口、地址和 `PAYLOAD` 中的 `FE` / `FF` 进行转义。
5. 连续发送 `SOF`、`LEN`、转义后的逻辑数据区和 `EOF`；帧内不得插入调试文本。

### 7.2 接收端伪代码

```text
state = WAIT_SOF
logical_count = 0
escape = false

on_byte(b):
  if state == WAIT_SOF:
      if b == 0xFE: state = WAIT_LEN
  elif state == WAIT_LEN:
      if 4 <= b <= 63:
          len = b; logical_count = 0; state = DATA
      else:
          reset()
  elif state == DATA:
      if escape:
          append((b + 2) & 0xFF); escape = false
      elif b == 0xFE:
          escape = true
      elif b == 0xFF:
          if logical_count == len: deliver(); reset()
          else: reset()
      else:
          append(b)
      if logical_count > len: reset()
```

稳健实现应仅接受 `FE FC` 与 `FE FD` 两种转义组合，其他转义后继字节视为格式错误。

## 8. 联调测试向量

| 编号 | 输入 / 目标 | 期望线上字节 | 验证点 |
|---|---|---|---|
| TV-01 | 目标 `0003`；`91 -> 90`；`00 AE 13 33` | `FE 08 91 90 03 00 00 AE 13 33 FF` | 小端地址，`LEN = 8`。 |
| TV-02 | 目标 `0003`；`91 -> 90`；`09 FF` | `FE 06 91 90 03 00 09 FE FD FF` | `FF` 转义，长度不变。 |
| TV-03 | 目标 `0002`；`A3 -> 20`；`32` | `FE 05 A3 20 02 00 32 FF` | LED 内部端口报文。 |
| TV-04 | 本地管理读地址 | `FE 05 90 21 00 00 01 FF` | 管理端口必须使用本地地址 `0000`。 |
| TV-05 | 非法源端口 `20` | `FE 06 22 20 02 00 E0 20 FF` | 识别 `0x22` 错误报告。 |

## 9. 工程检查清单

- [ ] 节点地址唯一；网络 ID 相同且不使用 `0x0000`、`0xFFFF`；信道均在 `0x0B`-`0x1A`。
- [ ] 业务源端口使用 `0x80`-`0xFF`，内部端口只作为目的端口。
- [ ] 长度按反转义前逻辑数据区计算，地址低字节先发。
- [ ] 发送端同时转义 `FE` 与 `FF`；接收端在 `EOF` 时严格校验逻辑字节数量。
- [ ] 模块启动后为路由收敛预留 1-5 分钟，未稳定期间不以稳定时延评估系统性能。
- [ ] 改变波特率、网络 ID 或信道前保存原配置，并准备 `BaudReset` 恢复路径。
- [ ] 在量产前，以原厂手册或实测补齐 LED 时间边界、`0x21` 写配置参数和 `0x22` 错误码。

## 10. 参考来源

优信电子，《基于 STM32F103最小系统板和DL-LN33 2.4G通信 ZigBee无线串口自组网采集温湿度》，CSDN 博客，2024-06-27（页面显示修改日期）。

<https://blog.csdn.net/qq_42250136/article/details/139967636>

