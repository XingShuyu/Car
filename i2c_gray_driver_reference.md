# I2C 灰度循迹驱动迁移参考

本文档总结当前示例工程中 NCHD12 十二路灰度传感器的 I2C 驱动、读数格式、结构体和偏差换算逻辑，供后续迁移到硬件 I2C、抽象总线层或重构设备驱动时参考。

## 1. 相关文件

| 文件 | 作用 |
| --- | --- |
| `ndrivers/soft_i2c.h` / `ndrivers/soft_i2c.c` | NCHD12 专用的软件模拟 I2C，总线层，直接操作 PA0/PA1。 |
| `ndrivers/nchd12.h` / `ndrivers/nchd12.c` | NCHD12/PCA9555/PCF8575 的设备读取层，把 I2C 返回的 2 字节数据合成为 12 位灰度状态。 |
| `ndrivers/gray_detection.h` / `ndrivers/gray_detection.c` | 灰度状态结构体、全局状态和 10mm/20mm 黑线偏差换算表。 |
| `main.c` | 初始化 OLED、检测灰度模块 ACK、循环读取灰度状态并显示/换算。 |
| `ti_msp_dl_config.h` | SysConfig 生成的引脚宏。NCHD12 使用 `GPIOA.0` 作为 SCL、`GPIOA.1` 作为 SDA。 |
| `硬件连接说明.txt` | 硬件接线与默认 I2C 地址说明。 |

## 2. 硬件连接和地址

### 2.1 引脚

NCHD12 灰度传感器使用独立的软件 I2C 引脚：

| MSPM0G3507 | NCHD12 |
| --- | --- |
| PA0 / `NCHD12_PORT_SCL_PIN` | SCL |
| PA1 / `NCHD12_PORT_SDA_PIN` | SDA |

OLED 也使用 I2C，但在另一组引脚上，由 `drv_oled.c` 内部的软件 I2C 驱动处理，不属于本灰度驱动的总线。

### 2.2 I2C 地址

硬件说明中写明：当灰度传感器地址焊盘全部悬空/低电平时，默认读地址为 `0x41`，写地址为 `0x40`。

这组地址是“8 位 I2C 控制字”，包含最低位 R/W 位：

| 含义 | 数值 |
| --- | --- |
| 7 位从机地址 | `0x20` |
| 8 位写控制字 | `0x40` = `0x20 << 1` |
| 8 位读控制字 | `0x41` = `(0x20 << 1) | 1` |

当前示例代码的软件 I2C API 使用的是 8 位控制字：

```c
i2c_CheckDevice(0x40);
gray_state.state = pca9555_read_bit12(0x20 << 1);
```

迁移到 MSPM0 DriverLib、HAL、RTOS I2C 或其他硬件 I2C API 时，要先确认 API 参数语义：

| API 类型 | 应传地址 |
| --- | --- |
| 当前 `soft_i2c` / 手动发送地址字节 | `0x40` 写，`0x41` 读 |
| 常见硬件 I2C API，参数为 7 位地址 | `0x20` |
| 常见硬件 I2C API，参数为 8 位控制字 | `0x40` / `0x41` |

建议重构时明确命名，避免把 7 位地址和 8 位控制字混用：

```c
#define NCHD12_I2C_ADDR_7BIT   0x20u
#define NCHD12_I2C_ADDR_WRITE  (NCHD12_I2C_ADDR_7BIT << 1)
#define NCHD12_I2C_ADDR_READ   ((NCHD12_I2C_ADDR_7BIT << 1) | 1u)
```

## 3. 软件 I2C 总线层

`soft_i2c.c` 对 PA0/PA1 进行位操作：

| 函数 | 作用 | 注意点 |
| --- | --- | --- |
| `_bsp_analog_i2c_start()` | 产生 START。 | SDA 输出，高到低；随后拉低 SCL。 |
| `_bsp_analog_i2c_stop()` | 产生 STOP。 | SCL 高电平期间 SDA 低到高。 |
| `_bsp_analog_i2c_send_byte()` | 发送 1 字节，MSB first，并自动等待 ACK。 | ACK 结果没有返回给上层。 |
| `_bsp_analog_i2c_send_byte_nask()` | 只发送 1 字节，不自动等待 ACK。 | 名字里的 `nask` 实际表示“不等待 ACK”。 |
| `_bsp_analog_i2c_wait_ack()` | 释放 SDA 后读取 ACK。 | 返回 `0` 表示 ACK 成功，返回 `1` 表示超时/NACK。 |
| `_bsp_analog_i2c_read_byte()` | 读取 1 字节，MSB first。 | 读完后由调用者发送 ACK 或 NACK。 |
| `_bsp_analog_i2c_ack()` | 主机 ACK。 | 用于连续读取中间字节后。 |
| `_bsp_analog_i2c_nack()` | 主机 NACK。 | 用于最后 1 字节后。 |
| `i2c_CheckDevice(uint8_t _Address)` | 发送地址并检测 ACK。 | 参数是 8 位写控制字；返回 `0` 成功、`1` 失败。 |

时序延时由 `_analog_i2c_delay()` 中的空循环决定，当前循环次数为 10。迁移到硬件 I2C 时建议先使用 100 kHz 或 400 kHz 标准速率调通，再根据波形和模块规格调整。

## 4. NCHD12/PCA9555 读取协议

当前 `main.c` 使用的是 `pca9555_read_bit12()`：

```c
uint16_t pca9555_read_bit12(uint8_t slave_num)
```

注意：这里的 `slave_num` 不是 7 位地址，而是 8 位写控制字，当前传入值为 `0x40`。

### 4.1 PCA9555 读取流程

`pca9555_read_bit12(0x40)` 的总线流程如下：

1. START
2. 发送写控制字 `0x40`
3. 等待 ACK
4. 发送寄存器地址 `INPUT_PORT_REGISTER0`，即 `0x00`
5. 等待 ACK
6. Repeated START
7. 发送读控制字 `0x41`
8. 等待 ACK
9. 读取低字节 `ldata`
10. 主机 ACK
11. 读取高字节 `hdata`
12. 主机 NACK
13. STOP
14. 合成结果：`((uint16_t)hdata << 8 | ldata) & 0x0FFF`

若改用硬件 I2C，等价操作通常是“写寄存器地址 0x00 后重复起始读 2 字节”：

```c
uint8_t buf[2];

// 伪代码：不同平台 API 名字不同
i2c_write_read(NCHD12_I2C_ADDR_7BIT, 0x00, buf, 2);

uint16_t raw12 = (((uint16_t)buf[1] << 8) | buf[0]) & 0x0FFFu;
```

### 4.2 PCF8575 读取流程

工程中还保留了 `pcf8575_read_bit12()`。它不写寄存器地址，直接读 2 字节：

1. START
2. 发送读控制字：`slave_num | 0x01`
3. 读取低字节 `ldata`
4. ACK
5. 读取高字节 `hdata`
6. NACK
7. STOP
8. 合成低 12 位：`((uint16_t)hdata << 8 | ldata) & 0x0FFF`

如果确认模块底层是 PCA9555，应继续使用寄存器地址 `0x00` 的读取方式；只有换成 PCF8575 类 IO 扩展器时才使用直接读。

## 5. PCA9555 寄存器定义

`nchd12.h` 中定义了 PCA9555 的常用寄存器：

| 宏 | 地址 | 含义 |
| --- | --- | --- |
| `INPUT_PORT_REGISTER0` | `0x00` | 输入端口 0，低 8 位。 |
| `INPUT_PORT_REGISTER1` | `0x01` | 输入端口 1，高 8 位。 |
| `OUTPUT_PORT_REGISTER0` | `0x02` | 输出端口 0。 |
| `OUTPUT_PORT_REGISTER1` | `0x03` | 输出端口 1。 |
| `POLARITY_INVERSION_PORT_REGISTER0` | `0x04` | 极性反转端口 0。 |
| `POLARITY_INVERSION_PORT_REGISTER1` | `0x05` | 极性反转端口 1。 |
| `CONFIG_PORT_REGISTER0` | `0x06` | 配置端口 0，PCA9555 中 `1` 通常表示输入。 |
| `CONFIG_PORT_REGISTER1` | `0x07` | 配置端口 1，PCA9555 中 `1` 通常表示输入。 |

当前示例没有主动写配置寄存器，依赖器件/模块上电默认输入状态。重构成更健壮的驱动时，可以在初始化阶段显式配置端口为输入，并按需要确认极性反转寄存器是否为默认值。

## 6. 灰度读取结果格式

读取函数返回值为 `uint16_t`，只使用低 12 位：

```c
raw12 = ((hdata << 8) | ldata) & 0x0FFF;
```

| 位 | 掩码 | 当前结构体字段 | 显示顺序 | 说明 |
| --- | --- | --- | --- | --- |
| bit0 | `0x0001` | `gray_state.gray.bit1` | 最右侧 | 第 1 路状态 |
| bit1 | `0x0002` | `gray_state.gray.bit2` | 右侧 | 第 2 路状态 |
| bit2 | `0x0004` | `gray_state.gray.bit3` | 右侧 | 第 3 路状态 |
| bit3 | `0x0008` | `gray_state.gray.bit4` | 右侧 | 第 4 路状态 |
| bit4 | `0x0010` | `gray_state.gray.bit5` | 右侧 | 第 5 路状态 |
| bit5 | `0x0020` | `gray_state.gray.bit6` | 中间偏右 | 第 6 路状态 |
| bit6 | `0x0040` | `gray_state.gray.bit7` | 中间偏左 | 第 7 路状态 |
| bit7 | `0x0080` | `gray_state.gray.bit8` | 左侧 | 第 8 路状态 |
| bit8 | `0x0100` | `gray_state.gray.bit9` | 左侧 | 第 9 路状态 |
| bit9 | `0x0200` | `gray_state.gray.bit10` | 左侧 | 第 10 路状态 |
| bit10 | `0x0400` | `gray_state.gray.bit11` | 左侧 | 第 11 路状态 |
| bit11 | `0x0800` | `gray_state.gray.bit12` | 最左侧 | 第 12 路状态 |
| bit12-bit15 | `0xF000` | `bit13`-`bit16` | 不使用 | 读取后被 `0x0FFF` 屏蔽。 |

硬件说明中写明 OLED 显示 `0` 表示低电平，`1` 表示高电平。当前算法直接使用原始电平，没有做软件取反；偏差表按“状态位为 1”来匹配黑线位置。

`main.c` 中 OLED 从左到右显示：

```c
bit12, bit11, bit10, bit9, bit8, bit7, bit6, bit5, bit4, bit3, bit2, bit1
```

因此按当前显示约定，`bit12` 在屏幕/传感器左侧，`bit1` 在右侧。偏差表中 `0x0001` 映射为 `-11`，`0x0800` 映射为 `+11`；迁移到控制算法时应按实际车体坐标确认正负方向是否需要反号。

## 7. 结构体和全局变量

### 7.1 位域结构体

```c
typedef struct
{
    uint8_t bit1  : 1;
    uint8_t bit2  : 1;
    ...
    uint8_t bit16 : 1;
} gray_flags;

typedef union
{
    uint16_t state;
    gray_flags gray;
} _gray_state;
```

全局实例：

```c
_gray_state gray_state;
```

`gray_state.state` 保存完整 16 位状态，其中低 12 位有效；`gray_state.gray.bitN` 用于显示每一路状态。

迁移注意：C 位域的位序和打包方式带有编译器相关性，当前工程在目标编译器下按预期工作，但重构成跨平台驱动时建议改用显式位操作：

```c
#define GRAY_CHANNEL_MASK(ch)   (1u << ((ch) - 1u))
#define GRAY_CHANNEL_GET(raw, ch) (((raw) >> ((ch) - 1u)) & 0x01u)
```

### 7.2 偏差状态变量

```c
float gray_status[2] = {0};
float gray_status_backup[2][20] = {0};
uint32_t gray_status_worse = 0;
```

| 变量 | 含义 |
| --- | --- |
| `gray_status[0]` | 10mm 黑线模式下的偏差值。 |
| `gray_status[1]` | 20mm 黑线模式下的偏差值。 |
| `gray_status_backup[0][20]` | 10mm 模式历史值，异常时用于沿用上一次有效结果。 |
| `gray_status_backup[1][20]` | 20mm 模式历史值，当前只维护，异常时没有使用历史值。 |
| `gray_status_worse` | 10mm 模式异常计数；匹配到有效状态时减半，异常状态时加一。 |

## 8. 偏差换算表

### 8.1 10mm 黑线

函数：`gpio_input_check_channel_12_linewidth_10mm()`

有效状态匹配到偏差后，`gray_status_worse /= 2`。`0x0000` 或其他未列出的组合会沿用上一帧 `gray_status_backup[0][0]`，并使 `gray_status_worse++`。

| raw12 | 二进制低 12 位 | `gray_status[0]` |
| --- | --- | --- |
| `0x0001` | `000000000001` | `-11` |
| `0x0003` | `000000000011` | `-10` |
| `0x0002` | `000000000010` | `-9` |
| `0x0006` | `000000000110` | `-8` |
| `0x0004` | `000000000100` | `-7` |
| `0x000C` | `000000001100` | `-6` |
| `0x0008` | `000000001000` | `-5` |
| `0x0018` | `000000011000` | `-4` |
| `0x0010` | `000000010000` | `-3` |
| `0x0030` | `000000110000` | `-2` |
| `0x0020` | `000000100000` | `-1` |
| `0x0060` | `000001100000` | `0` |
| `0x0040` | `000001000000` | `1` |
| `0x00C0` | `000011000000` | `2` |
| `0x0080` | `000010000000` | `3` |
| `0x0180` | `000110000000` | `4` |
| `0x0100` | `000100000000` | `5` |
| `0x0300` | `001100000000` | `6` |
| `0x0200` | `001000000000` | `7` |
| `0x0600` | `011000000000` | `8` |
| `0x0400` | `010000000000` | `9` |
| `0x0C00` | `110000000000` | `10` |
| `0x0800` | `100000000000` | `11` |

### 8.2 20mm 黑线

函数：`gpio_input_check_channel_12_linewidth_20mm()`

20mm 模式允许更多三连位组合。`0x0000` 或未列出的组合直接令 `gray_status[1] = 0`。

| raw12 | 二进制低 12 位 | `gray_status[1]` |
| --- | --- | --- |
| `0x0001` | `000000000001` | `-11` |
| `0x0003` | `000000000011` | `-10` |
| `0x0002` | `000000000010` | `-9` |
| `0x0007` | `000000000111` | `-9` |
| `0x0006` | `000000000110` | `-8` |
| `0x0004` | `000000000100` | `-7` |
| `0x000E` | `000000001110` | `-7` |
| `0x000C` | `000000001100` | `-6` |
| `0x0008` | `000000001000` | `-5` |
| `0x001C` | `000000011100` | `-5` |
| `0x0018` | `000000011000` | `-4` |
| `0x0010` | `000000010000` | `-3` |
| `0x0038` | `000000111000` | `-3` |
| `0x0030` | `000000110000` | `-2` |
| `0x0020` | `000000100000` | `-1` |
| `0x0070` | `000001110000` | `-1` |
| `0x0060` | `000001100000` | `0` |
| `0x0040` | `000001000000` | `1` |
| `0x00E0` | `000011100000` | `1` |
| `0x00C0` | `000011000000` | `2` |
| `0x0080` | `000010000000` | `3` |
| `0x01C0` | `000111000000` | `3` |
| `0x0180` | `000110000000` | `4` |
| `0x0100` | `000100000000` | `5` |
| `0x0380` | `001110000000` | `5` |
| `0x0300` | `001100000000` | `6` |
| `0x0200` | `001000000000` | `7` |
| `0x0700` | `011100000000` | `7` |
| `0x0600` | `011000000000` | `8` |
| `0x0400` | `010000000000` | `9` |
| `0x0E00` | `111000000000` | `9` |
| `0x0C00` | `110000000000` | `10` |
| `0x0800` | `100000000000` | `11` |

## 9. 主循环数据流

`main.c` 的流程：

1. `SYSCFG_DL_init()` 初始化芯片资源和 GPIO。
2. `oled_init()` 初始化 OLED。
3. `i2c_CheckDevice(0x40)` 检测灰度模块 ACK。
4. 循环中先显示上一帧的 `gray_state` 和 `gray_status`。
5. 翻转 LED。
6. `gray_state.state = pca9555_read_bit12(0x20 << 1)` 读取当前帧原始 12 位状态。
7. 调用 10mm 和 20mm 偏差换算函数更新 `gray_status[0]`、`gray_status[1]`。

因为显示代码在读取代码之前执行，所以 OLED 上看到的是上一轮循环的计算结果。若重构为实时任务，可以调整为“先读取/计算，后显示/控制输出”。

## 10. 重构建议

建议把当前代码拆成三层：

| 层级 | 建议接口 | 职责 |
| --- | --- | --- |
| I2C 总线层 | `i2c_write_read(addr7, reg, buf, len)`、`i2c_probe(addr7)` | 屏蔽软件 I2C/硬件 I2C 差异，统一使用 7 位地址。 |
| NCHD12 设备层 | `nchd12_init()`、`nchd12_read_raw12()`、`nchd12_get_channel(raw, ch)` | 处理地址、寄存器、2 字节合成、低 12 位屏蔽。 |
| 灰度算法层 | `gray_calc_offset_10mm(raw)`、`gray_calc_offset_20mm(raw)` | 只关心 `raw12`，不直接访问 I2C。 |

迁移时优先保留的行为：

- 默认 7 位地址为 `0x20`。
- PCA9555 读取寄存器从 `0x00` 开始，连续读 2 字节。
- 数据顺序为低字节在前、高字节在后。
- 只使用低 12 位：`raw12 & 0x0FFF`。
- `bit1` 对应 `0x0001`，`bit12` 对应 `0x0800`。
- 10mm 和 20mm 模式的偏差表可以先原样迁移，调通后再做表驱动化。

需要改进的点：

- 明确函数参数是 7 位地址还是 8 位控制字。
- `pca9555_read_bit12()` 当前忽略 ACK 失败，建议返回错误码或布尔状态。
- 位域结构体不适合作为跨编译器数据协议，建议改用 mask/shift。
- 初始化阶段建议显式配置 PCA9555 输入寄存器和极性寄存器。
- 显示、采样、算法、控制输出建议分离，避免主循环顺序造成一帧延迟或耦合。
